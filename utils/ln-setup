#!/bin/bash

etc=$HOME/all/Documents/etc

# link dot files
ln -sf "$etc/bash_aliases" "$HOME/.bash_aliases"
ln -sf "$etc/bashrc" "$HOME/.bashrc"
ln -sf "$etc/eslintrc.json"  "$HOME/.eslintrc.json"
ln -sf "$etc/gitconfig"  "$HOME/.gitconfig"
ln -sf "$etc/inputrc" "$HOME/.inputrc"
ln -sf "$etc/npmrc"  "$HOME/.npmrc"
ln -sf "$etc/profile" "$HOME/.profile"
ln -sf "$etc/pylintrc"  "$HOME/.pylintrc"
ln -sf "$etc/vimrc"  "$HOME/.vimrc"

# Link major folders to home dir
ln -sf "$HOME/all/Documents" "$HOME/Documents"
ln -sf "$HOME/all/Media/Phone" "$HOME/Pictures"
ln -sf "$HOME/all/Media/GoPro" "$HOME/Videos"
ln -sf "$HOME/all/Dropbox" "$HOME/Dropbox"

# Link common shortcuts to home dir
ln -sf "$HOME/all/Documents" "$HOME/d"
ln -sf "$HOME/all/Documents/etc" "$HOME/etc"
ln -sf "$HOME/all/Documents/go" "$HOME/go"
ln -sf "$HOME/all/Documents/journal" "$HOME/j"
ln -sf "$HOME/all/Documents/notes" "$HOME/n"
ln -sf "$HOME/all/Documents/todo" "$HOME/t"
ln -sf "$HOME/all/Documents/todo" "$HOME/.todo"
ln -sf "$HOME/all/Dropbox/Shared" "$HOME/s"

# Put misc utility scripts into our path
mkdir -p "$HOME/bin"

for util in $etc/utils/*
do
  ln -sf "$util" "$HOME/bin/`basename $util`"
done

ln -sf "$HOME/Documents/journal/rjournal" "$HOME/bin/rjournal"
ln -sf "$HOME/Documents/todo/todo.sh" "$HOME/bin/todo"


go=/usr/local/go/bin/go
[[ -f "$go" ]] && ln -sf "$go" "$HOME/bin/go"

